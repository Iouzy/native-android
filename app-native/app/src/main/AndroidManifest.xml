<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions are declared up front; the components that use them (focus
         foreground service, reminder alarms, boot re-arm, widget, tile) are
         added to this manifest as their phases land. // PT: permissões já
         declaradas; os componentes entram quando cada fase os implementa. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- The in-app updater is the app's only network use; it installs the APK it
         downloads via the system package installer. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".PautaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pauta"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Pauta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Launcher shortcut / QS tile route here to jump straight to focus. -->
            <intent-filter>
                <action android:name="com.pauta.app.SHORTCUT_FOCUS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <!-- C4: text share target — another app's "Share → Pauta" lands here,
                 confirmed and added as today's intention. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- C4: static launcher shortcuts (Nova intenção / Começar bloco /
                 Marés). The two new actions are delivered by explicit shortcut
                 intents, so they need no intent-filter of their own. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- R8: the second door. One app, one database, one task — the alias just
             launches MainActivity through a different component, which the activity
             reads on a cold start to set the `bookMode` preference. It is a shortcut
             to the in-app toggle, not a parallel mode.

             enabled="true" and never touched by PackageManager: disabling a
             component is how a home-screen shortcut is lost for good, and both
             icons must survive an OTA update. launchMode is inherited from the
             target, so both doors resolve to the same task and the app appears once
             in recents. // PT: segundo ícone — a mesma Activity por outro
             componente; só liga a preferência de modo livro. -->
        <activity-alias
            android:name=".BookLauncher"
            android:targetActivity=".MainActivity"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_book"
            android:roundIcon="@mipmap/ic_launcher_book_round"
            android:label="@string/app_name_book">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Foreground service owning the ongoing focus-timer notification. -->
        <service
            android:name=".service.FocusService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_timer" />
        </service>

        <!-- R2 (§2 of the reader's security model): attached books are parsed in
             their own process. A malformed PDF can abort PdfRenderer natively —
             a crash Kotlin cannot catch — so it takes :reader down and leaves the
             app, its data and a running focus block untouched. Not exported: no
             other app can reach it. // PT: os livros anexados são lidos noutro
             processo, para um crash nativo não levar a app com ele. -->
        <service
            android:name=".service.DocumentParseService"
            android:process=":reader"
            android:exported="false" />

        <!-- Notification action buttons (pause / conclude). -->
        <receiver
            android:name=".service.FocusActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.pauta.app.FOCUS_PAUSE" />
                <action android:name="com.pauta.app.FOCUS_CONCLUDE" />
            </intent-filter>
        </receiver>

        <!-- Daily reminder alarm receiver. -->
        <receiver
            android:name=".service.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.pauta.app.REMINDER_FIRE" />
            </intent-filter>
        </receiver>

        <!-- C2: actionable reminder notification buttons — the reflection
             direct-reply and the "Feito" tide actions route here (explicit
             intents, so no intent-filter is needed). -->
        <receiver
            android:name=".service.ReminderActionReceiver"
            android:exported="false" />

        <!-- C2: fires the one-time focus "target reached" alert from the lock
             screen when a running block hits its soft target. -->
        <receiver
            android:name=".service.FocusTargetReceiver"
            android:exported="false" />

        <!-- D2: per-habit reminder alarms fire here (explicit intent, so no
             intent-filter is needed); the receiver re-arms the next day and posts
             only if the tide is still due + unmarked. -->
        <receiver
            android:name=".service.HabitReminderReceiver"
            android:exported="false" />

        <!-- Re-arm reminders after reboot / app update. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- C1: Glance Marés home-screen widget — today's tides, tappable. Replaces
             the old three-line text widget (PautaWidgetProvider). -->
        <receiver
            android:name=".service.MaresWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/mares_widget_info" />
        </receiver>

        <!-- Shares exported backup JSON files. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Quick-Settings tile: tap to jump into focus. -->
        <service
            android:name=".service.FocusTileService"
            android:exported="true"
            android:icon="@drawable/ic_stat_focus"
            android:label="@string/focus_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

    </application>
</manifest>
